matrix = textread('books_pos');
matrix2 = textread('books_neg');

for k = 1:100:1000

    
a = [matrix([1:1:k-1, k+100:1:1000],:);matrix2([1:1:k-1, k+100:1:1000], :)];

b = [ones(900); ones()*-1];

w = perceptron(a,b);

count = 0;
for i = 950 : 1050,
    if sign(w' * a(i,:)') == b(i)  % y(i) x(i) check correct
        count = count + 1;
    end
end